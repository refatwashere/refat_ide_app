// Terminal controls widget
import 'package:flutter/material.dart';

class TerminalControls extends StatelessWidget {
  const TerminalControls({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Terminal Controls'),
    );
  }
}
