// Tab bar for file tabs
import 'package:flutter/material.dart';

class EditorTabBar extends StatelessWidget {
  const EditorTabBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Tab Bar'),
    );
  }
}
