// Editor pane widget
import 'package:flutter/material.dart';

class EditorPane extends StatelessWidget {
  const EditorPane({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Editor Pane'),
    );
  }
}
